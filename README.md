# Universal Research → Data → Build Surface v15

Production-oriented cross-host Agent Skill package with a lightweight automatic Skill routing layer.

## v15

- Dedicated `skill-router` control plane.
- Applicability-aware `use / sequence / abstain / ask / blocked` routing.
- Metadata-first progressive disclosure.
- Smallest-sufficient Skill selection and negative-transfer checks.
- Portable Agent Plugins 1.0.0 manifest.
- Codex-style `.agents/skills/` layout.

The complete v15 package archive is retained as the release artifact. The repository bootstrap files below make the routing contract explicit.